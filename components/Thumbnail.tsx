import { Movie } from '@/typings'
import React from 'react'

interface Props {
    movie: Movie
}

const Thumbnail = ({ movie }: Props) => {
  return (
    <div>
      Thumbnail
    </div>
  )
}

export default Thumbnail
